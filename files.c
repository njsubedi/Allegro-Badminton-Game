/*int savedata(char userdata[], int score)
{
    FILE *fopen();

    file_name = username;
    file_saver = fopen(file_name, "rt" );
    if(file_saver == NULL){
            puts(score, file_saver);
            return true;
        }
    else{
            return false;
        }
}

char readdata(char username[])
{
    FILE *fopen();
    file_name = username;
    file_reader = fopen(file_name, "r");
    return(gets(file_reader));
}
*/
